{
  "name": "@kaspa-x402/client",
  "version": "0.0.0",
  "private": true,
  "description": "Client SDK for Kaspa x402.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm --workspace @kaspa-x402/core run build && npm --workspace @kaspa-x402/covenant run build && tsup src/index.ts --format esm --dts --clean",
    "test": "npm --workspace @kaspa-x402/core run build && npm --workspace @kaspa-x402/covenant run build && vitest run"
  },
  "dependencies": {
    "@kaspa-x402/core": "0.0.0",
    "@kaspa-x402/covenant": "0.0.0"
  }
}
