{
  "kind": "tx-v1-plan",
  "description": "Required transaction v1 vectors before mainnet covenant-backed support is complete.",
  "coveredVectors": [
    {
      "name": "batch-claim-transaction-body",
      "path": "vectors/tx-v1/batch-claim.json",
      "description": "Reference transaction-v1 artifact that claims the active charged amount and creates the continuation output."
    },
    {
      "name": "batch-refund-transaction-body",
      "path": "vectors/tx-v1/batch-refund.json",
      "description": "Reference transaction-v1 artifact that refunds remaining escrow value after timeout."
    }
  ],
  "requiredFutureVectors": [
    {
      "name": "upto-settlement-transaction-body",
      "description": "Serialized transaction v1 body that consumes an upto authorization outpoint and pays the actual amount."
    },
    {
      "name": "upto-zero-charge-no-transaction-response",
      "description": "SettlementResponse shape for the explicit zero-charge/no-value-moved success path."
    },
    {
      "name": "transaction-id",
      "description": "Canonical transaction id for remaining upto transaction bodies."
    },
    {
      "name": "tx-hash",
      "description": "Canonical tx::hash value for remaining upto transaction bodies."
    },
    {
      "name": "sighash",
      "description": "Signature hash preimage and digest for every remaining upto signed input path."
    },
    {
      "name": "compute-budget",
      "description": "compute_budget values required for remaining upto paths."
    },
    {
      "name": "script-units-estimate",
      "description": "Estimated script unit cost for remaining upto settlement paths."
    }
  ]
}
